# Keep backend/requirements.txt and backend/api/requirements.txt byte-identical.
# Vercel's @vercel/python reads the copy next to the entry point (api/index.py);
# local dev uses this one. CI asserts the two files match exactly.
Flask==3.0.2
Flask-Cors==4.0.0
Flask-Caching==2.1.0
Flask-Limiter==3.8.0
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.32
redis==5.0.8
psycopg2-binary==2.9.9
gunicorn==21.2.0
python-dotenv==1.0.1
google-search-results==2.4.2
